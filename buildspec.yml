version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
      - echo Running composer...
      - echo next action... Done.
  post_build:
    commands:
      - echo Build completed on `date`
      
